{
  "private": true,
  "name": "nft-graph",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev-fe": "cd packages/frontend && npm run dev",
    "compile": "cd packages/contracts && npm run compile",
    "deploy": "cd packages/contracts && npm run deploy"
  },
  "workspaces": [
    "packages/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.5"
  },
  "dependencies": {
    "react-router-dom": "^6.23.1"
  }
}
